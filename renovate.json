{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],

  "prHourlyLimit": 0,

  "argocd": {
    "managerFilePatterns": [
      "/resources/gitops-config/sync-app/templates/.*\\.yaml/"
    ]
  },

  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/resources/gitops-config/operators/cloudnative-pg/templates/.*\\.yaml/"
      ],
      "matchStrings": ["imageName: (?<depName>[^\\s:]+):(?<currentValue>[^@\\s]+)(?:@(?<currentDigest>sha256:[a-f0-9]+))?"],
      "datasourceTemplate": "docker"
    },
    {
      "description": "Track hardcoded utility images in Helm template files",
      "customType": "regex",
      "managerFilePatterns": [
        "/resources/gitops-config/.*/templates/.*\\.yaml/"
      ],
      "matchStrings": ["image: (?<depName>busybox|redis|alpine/k8s|ghcr\\.io/coder/code-server):(?<currentValue>[^\\s\"]+)"],
      "datasourceTemplate": "docker"
    }
  ],

  "packageRules": [
    {
      "description": "Add [Tier: 0] prefix to tier-0 PR titles",
      "matchManagers": ["argocd"],
      "matchPackageNames": ["argo-cd", "argocd-apps", "cilium", "longhorn"],
      "commitMessagePrefix": "[Tier: 0]",
      "schedule": ["before 6am on Monday"]
    },
    {
      "description": "Add [Tier: 1] prefix to tier-1 PR titles",
      "matchManagers": ["argocd"],
      "matchPackageNames": [
        "cert-manager", "cloudnative-pg", "external-dns",
        "external-secrets", "kubelet-csr-approver", "connect"
      ],
      "commitMessagePrefix": "[Tier: 1]",
      "schedule": ["before 6am on Monday"]
    },
    {
      "description": "Group tier-2+ patch updates into a single automerged PR",
      "matchManagers": ["argocd", "helm-values"],
      "matchUpdateTypes": ["patch"],
      "groupName": "Helm charts (tier-2+) - patches",
      "commitMessagePrefix": "[Tier: 2+]",
      "automerge": true,
      "matchPackageNames": [
        "!argo-cd",
        "!argocd-apps",
        "!cilium",
        "!longhorn",
        "!cert-manager",
        "!cloudnative-pg",
        "!external-dns",
        "!external-secrets",
        "!kubelet-csr-approver",
        "!connect",
        "!kube-prometheus-stack"
      ]
    },
    {
      "description": "Individual PRs for tier-2+ minor and major updates",
      "matchManagers": ["argocd", "helm-values"],
      "matchUpdateTypes": ["minor", "major"],
      "commitMessagePrefix": "[Tier: 2+]",
      "matchPackageNames": [
        "!argo-cd",
        "!argocd-apps",
        "!cilium",
        "!longhorn",
        "!cert-manager",
        "!cloudnative-pg",
        "!external-dns",
        "!external-secrets",
        "!kubelet-csr-approver",
        "!connect"
      ]
    },
    {
      "description": "Pin digests for CNPG PostgreSQL images tracked via customManager",
      "matchManagers": ["custom.regex"],
      "matchFileNames": ["resources/gitops-config/operators/cloudnative-pg/templates/**"],
      "matchDepNames": ["ghcr.io/cloudnative-pg/postgresql"],
      "pinDigests": true
    },
    {
      "description": "Block emqx-enterprise upgrades to 6.x (BSL license change, clustering costs)",
      "matchPackageNames": ["emqx-enterprise"],
      "allowedVersions": ">=5.9.0 <6.0.0"
    },
    {
      "description": "Disable patch updates for kube-prometheus-stack",
      "matchPackageNames": ["kube-prometheus-stack"],
      "matchUpdateTypes": ["patch"],
      "enabled": false
    },
    {
      "description": "Group all Terraform provider updates",
      "matchManagers": ["terraform"],
      "groupName": "Terraform providers",
      "commitMessagePrefix": "[Tier: Infra]"
    }
  ]
}
