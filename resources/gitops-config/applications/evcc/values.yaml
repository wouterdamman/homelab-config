# EVCC Configuration
# Solar charging for electric vehicles

image:
  repository: evcc/evcc
  tag: "0.300.5"
  pullPolicy: IfNotPresent

replicas: 1

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

storage:
  size: 2Gi
  storageClass: longhorn-standard

service:
  type: ClusterIP
  port: 7070

ingress:
  enabled: true
  hostname: evcc.app.damman.tech
  className: cilium

# Configuration via ExternalSecret from 1Password
# Config is copied from secret to /data/evcc.yaml on startup
externalSecrets:
  enabled: true
  items:
  - name: evcc-config
    secretStoreRef: onepassword-connect
    data:
    - remoteKey: evcc-config
      secretKey: evcc.yaml
