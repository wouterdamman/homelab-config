apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "home-assistant.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "home-assistant.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "home-assistant.fullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "home-assistant.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "home-assistant.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.hostNetwork }}
      hostNetwork: true
      {{- end }}
      {{- if .Values.dnsPolicy }}
      dnsPolicy: {{ .Values.dnsPolicy }}
      {{- end }}

      # Pod-level security context
      securityContext:
        fsGroup: 911
        fsGroupChangePolicy: "OnRootMismatch"

      # Init container to fix permissions for code-server
      initContainers:
      - name: fix-permissions
        image: busybox:1.37.0
        command:
        - sh
        - -c
        - |
          echo "Fixing /config permissions for UID 911 (abc user)..."
          chown -R :911 /config
          chmod -R g+rw /config
          find /config -type d -exec chmod g+s {} \;
          echo "Permissions fixed successfully"
        volumeMounts:
        - name: config
          mountPath: /config
        securityContext:
          runAsUser: 0
          runAsGroup: 0

      containers:
      # Home Assistant main container
      - name: home-assistant
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        # Container-level security context - override pod-level to run as root
        # This is needed for Home Assistant to install Python packages
        securityContext:
          runAsUser: 0
          runAsGroup: 0
        ports:
        - name: http
          containerPort: 8123
          protocol: TCP
        {{- if .Values.env }}
        env:
        {{- toYaml .Values.env | nindent 8 }}
        {{- end }}
        volumeMounts:
        - name: config
          mountPath: /config
        {{- if .Values.externalSecrets.enabled }}
        {{- range .Values.externalSecrets.items }}
        {{- if not .coderOnly }}
        - name: {{ .name }}
          mountPath: /config/{{ .secretKey }}
          subPath: {{ .secretKey }}
          readOnly: true
        {{- end }}
        {{- end }}
        {{- end }}
        {{- if .Values.resources }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        {{- end }}
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

      {{- if .Values.addons.codeserver.enabled }}
      # Code-server sidecar container
      - name: codeserver
        image: ghcr.io/coder/code-server:4.118.0
        imagePullPolicy: IfNotPresent
        args:
        - --auth
        - none
        - --user-data-dir
        - /config/.vscode
        - --extensions-dir
        - /config/.vscode-extensions
        - /config
        ports:
        - name: codeserver
          containerPort: 8080
          protocol: TCP
        volumeMounts:
        - name: config
          mountPath: /config
        {{- if .Values.externalSecrets.enabled }}
        {{- range .Values.externalSecrets.items }}
        - name: {{ .name }}
          mountPath: {{ default (printf "/config/%s" .secretKey) .mountPath }}
          subPath: {{ .secretKey }}
          readOnly: true
        {{- end }}
        {{- end }}
        {{- if .Values.sshKnownHosts.enabled }}
        - name: ssh-known-hosts
          mountPath: /home/coder/.ssh/known_hosts
          subPath: known_hosts
          readOnly: true
        {{- end }}
        env:
        - name: TZ
          value: Europe/Amsterdam
        {{- if .Values.addons.codeserver.resources }}
        resources:
          {{- toYaml .Values.addons.codeserver.resources | nindent 10 }}
        {{- end }}
      {{- end }}

      volumes:
      {{- if .Values.externalSecrets.enabled }}
      {{- range .Values.externalSecrets.items }}
      - name: {{ .name }}
        secret:
          secretName: {{ .name }}
          defaultMode: {{ default 0440 .defaultMode }}
      {{- end }}
      {{- end }}
      {{- if .Values.sshKnownHosts.enabled }}
      - name: ssh-known-hosts
        configMap:
          name: {{ include "home-assistant.fullname" . }}-ssh-known-hosts
      {{- end }}

  volumeClaimTemplates:
  - metadata:
      name: config
    spec:
      accessModes: [ "ReadWriteOnce" ]
      {{- if .Values.persistence.storageClassName }}
      storageClassName: {{ .Values.persistence.storageClassName }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.persistence.size | default "10Gi" }}
