---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage-authentik-outpost-token
  namespace: {{ .Release.Namespace }}
spec:
  secretStoreRef:
    name: {{ .Values.authentikOutpost.externalSecret.secretStoreRef }}
    kind: ClusterSecretStore
  target:
    name: homepage-authentik-outpost-token
  data:
  - secretKey: token
    remoteRef:
      key: {{ .Values.authentikOutpost.externalSecret.remoteKey }}
      property: token
---
# One ExternalSecret per widget credential — kept separate (not bundled
# into a single homepage-secrets Secret) so a missing/not-yet-created
# 1Password item only breaks that one widget's env vars instead of
# blocking every widget's secret sync at once.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage-proxmox-secret
  namespace: {{ .Release.Namespace }}
spec:
  secretStoreRef:
    name: {{ .Values.widgetSecrets.secretStoreRef }}
    kind: ClusterSecretStore
  target:
    name: homepage-proxmox-secret
    template:
      engineVersion: v2
      data:
        # "Proxmox - Automation Account".api_key holds "tokenid=secret"
        # (e.g. automation@pve!tofu=<uuid>) — tokenId itself is not secret
        # (hardcoded in values.yaml services.yaml), only the part after "=" is.
        HOMEPAGE_VAR_PROXMOX_TOKEN: '{{ `{{ index (splitn "=" 2 .proxmoxApiKey) "_1" }}` }}'
  data:
  - secretKey: proxmoxApiKey
    remoteRef:
      key: {{ .Values.widgetSecrets.proxmox.remoteKey }}
      property: api_key
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage-adguard-secret
  namespace: {{ .Release.Namespace }}
spec:
  secretStoreRef:
    name: {{ .Values.widgetSecrets.secretStoreRef }}
    kind: ClusterSecretStore
  target:
    name: homepage-adguard-secret
    template:
      engineVersion: v2
      data:
        HOMEPAGE_VAR_ADGUARD_USER: '{{ `{{ .adguardUser }}` }}'
        HOMEPAGE_VAR_ADGUARD_PASSWORD: '{{ `{{ .adguardPassword }}` }}'
  data:
  - secretKey: adguardUser
    remoteRef:
      key: {{ .Values.widgetSecrets.adguard.remoteKey }}
      property: username
  - secretKey: adguardPassword
    remoteRef:
      key: {{ .Values.widgetSecrets.adguard.remoteKey }}
      property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage-argocd-secret
  namespace: {{ .Release.Namespace }}
spec:
  secretStoreRef:
    name: {{ .Values.widgetSecrets.secretStoreRef }}
    kind: ClusterSecretStore
  target:
    name: homepage-argocd-secret
    template:
      engineVersion: v2
      data:
        HOMEPAGE_VAR_ARGOCD_TOKEN: '{{ `{{ .argocdToken }}` }}'
  data:
  - secretKey: argocdToken
    remoteRef:
      key: {{ .Values.widgetSecrets.argocd.remoteKey }}
      property: token
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage-grafana-secret
  namespace: {{ .Release.Namespace }}
spec:
  secretStoreRef:
    name: {{ .Values.widgetSecrets.secretStoreRef }}
    kind: ClusterSecretStore
  target:
    name: homepage-grafana-secret
    template:
      engineVersion: v2
      data:
        HOMEPAGE_VAR_GRAFANA_USER: '{{ `{{ .grafanaUser }}` }}'
        HOMEPAGE_VAR_GRAFANA_PASSWORD: '{{ `{{ .grafanaPassword }}` }}'
  data:
  - secretKey: grafanaUser
    remoteRef:
      key: {{ .Values.widgetSecrets.grafana.remoteKey }}
      property: username
  - secretKey: grafanaPassword
    remoteRef:
      key: {{ .Values.widgetSecrets.grafana.remoteKey }}
      property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage-authentik-secret
  namespace: {{ .Release.Namespace }}
spec:
  secretStoreRef:
    name: {{ .Values.widgetSecrets.secretStoreRef }}
    kind: ClusterSecretStore
  target:
    name: homepage-authentik-secret
    template:
      engineVersion: v2
      data:
        HOMEPAGE_VAR_AUTHENTIK_TOKEN: '{{ `{{ .authentikToken }}` }}'
  data:
  - secretKey: authentikToken
    remoteRef:
      key: {{ .Values.widgetSecrets.authentik.remoteKey }}
      property: token
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homepage-homeassistant-secret
  namespace: {{ .Release.Namespace }}
spec:
  secretStoreRef:
    name: {{ .Values.widgetSecrets.secretStoreRef }}
    kind: ClusterSecretStore
  target:
    name: homepage-homeassistant-secret
    template:
      engineVersion: v2
      data:
        HOMEPAGE_VAR_HOMEASSISTANT_TOKEN: '{{ `{{ .homeassistantToken }}` }}'
  data:
  - secretKey: homeassistantToken
    remoteRef:
      key: {{ .Values.widgetSecrets.homeassistant.remoteKey }}
      property: token
