{{- if .Values.sso.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: netbox-config
  namespace: netbox
data:
  extra.py: |
    import os

    # OIDC/OAuth2 Configuration for Authentik
    REMOTE_AUTH_ENABLED = True
    REMOTE_AUTH_BACKEND = 'social_core.backends.open_id_connect.OpenIdConnectAuth'
    REMOTE_AUTH_AUTO_CREATE_USER = True

    # Social auth configuration
    AUTHENTICATION_BACKENDS = (
        'social_core.backends.open_id_connect.OpenIdConnectAuth',
        'netbox.authentication.RemoteUserBackend',
    )

    # OIDC settings
    SOCIAL_AUTH_OIDC_OIDC_ENDPOINT = '{{ .Values.sso.issuerUrl }}'
    SOCIAL_AUTH_OIDC_KEY = '{{ .Values.sso.clientId }}'
    SOCIAL_AUTH_OIDC_SECRET = os.environ.get('SOCIAL_AUTH_OIDC_SECRET')

    # Login URLs
    LOGIN_URL = '/oauth/login/oidc/'
    LOGIN_REDIRECT_URL = '/'

    # Additional OIDC configuration
    SOCIAL_AUTH_OIDC_SCOPE = {{ .Values.sso.scopes | splitList " " | toJson }}
    SOCIAL_AUTH_OIDC_USERNAME_KEY = 'preferred_username'

    # Custom pipeline to handle missing user fields
    def ensure_user_fields(backend, details, response, user=None, *args, **kwargs):
        """Ensure required user fields have values"""
        if not details.get('last_name'):
            # Use first_name as last_name if family_name is missing
            details['last_name'] = details.get('first_name', 'User')
        if not details.get('first_name'):
            details['first_name'] = details.get('username', 'Unknown')
        return {'details': details}

    SOCIAL_AUTH_PIPELINE = (
        'social_core.pipeline.social_auth.social_details',
        'social_core.pipeline.social_auth.social_uid',
        'social_core.pipeline.social_auth.auth_allowed',
        'social_core.pipeline.social_auth.social_user',
        'social_core.pipeline.user.get_username',
        ensure_user_fields,  # Custom pipeline step (function reference)
        'social_core.pipeline.user.create_user',
        'social_core.pipeline.social_auth.associate_user',
        'social_core.pipeline.social_auth.load_extra_data',
        'social_core.pipeline.user.user_details',
    )

    # User attribute mapping
    SOCIAL_AUTH_OIDC_USER_FIELDS = {
        'username': 'preferred_username',
        'email': 'email',
        'first_name': 'given_name',
        'last_name': 'family_name',
    }
{{- end }}
