connect:
  credentialsName: onepassword-connect-credentials
  credentialsKey: onepassword-connect-credentials.json

  # API container resources
  api:
    resources:
      limits:
        cpu: 200m
        memory: 128Mi
      requests:
        cpu: 20m
        memory: 32Mi

  # Sync container resources
  sync:
    resources:
      limits:
        cpu: 200m
        memory: 128Mi
      requests:
        cpu: 20m
        memory: 32Mi
